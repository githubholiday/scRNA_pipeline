upload
└── *space
    ├── *_spatial_cluster_umap.p*
└── *RNA
    ├── *_singlecell_cluster_umap.p*
    ├── *_celltype_count.xls
└── *Integrate
    ├── *_boxplot_*.p*
    ├── *single_celltype*.png
    ├── *_CorrelationMatrix.p*
    ├── *_CorrelationMatrix.xls
    ├── *_Interactions.p*
    ├── *_Interactions.xls
    ├── *_res.rds
    ├── *_all_spot_celltype.xls
    ├── *_all_spot_celltype_example.xls
    └── *_SpatialScatterpie*.p*

（1）*_spatial_cluster_umap.p*
空间转录组的聚类结果umap图

（2）*_singlecell_cluster_umap.p*
单细胞10X的注释结果umap图

（3）*_celltype_count.xls
单细胞10X的注释结果统计。
celltype：细胞类型；
count：该细胞类型的细胞数量。

（4）*_all_spot_celltype.xls、*_all_spot_celltype_example.xls
*_all_spot_celltype_example.xls为*_all_spot_celltype.xls的示例文件，用于报告展示。
spot：barcode ID；
第二行/列-：不同的细胞类型在该spot中的占比。

（5）*_SpatialScatterpie*.p*
*_SpatialScatterpie_*.p*为每个cluter分别展示的饼图；*_SpatialScatterpie.p*为所有的cluster共同展示的饼图，*_SpatialScatterpie_nobackground.p*为该图的无背景图。
每一个饼代表一个spot，不同的颜色为不同的细胞类型。

（6）*single_celltype*.png
每种细胞类型在所有spot中的占比情况。颜色代表该细胞类型在该spot中的占比，颜色越红，代表占比越大。

（7）*_boxplot_*.p*
不同cluster中细胞类型占比箱式图。一个细胞绘制一个图；横坐标是空间的spot cluster分类。纵坐标是spot中的该细胞类型所占的百分比。

（8）*_CorrelationMatrix.p*
同细胞类型在空间分布上的相似性。值为正，说明两种细胞类型在空间上的分布趋势是正相关的，越接近1，分布趋势越相似；值为负，代表负相关，两种细胞类型在空间上的分布趋势是相反的，越接近-1，分布趋势相反。

（9）*_CorrelationMatrix.xls
*_CorrelationMatrix.p*图的作图数据。代表细胞类型之间的相关性系数。

（10）*_Interactions.p*
细胞类型间的空间共定位。线的宽度表示的是两种细胞在空间上的相近性，线越粗，表示两个细胞在空间上越相近。

（11）*_Interactions.xls
细胞类型间的空间共定位的作图文件。第一列和第二列为细胞类型，第三列为这两种细胞类型共同出现的spot的数量，第四列为对第三列均一化处理之后的数值。第四列对应了图中线条的粗细。如果数值相近，则粗细体现的不明显。

（12）*_res.rds
SPOTlight解卷积的结果。可以用R打开，包括*_all_spot_celltype.xls等结果，是一个list对象。示例命令：res = readRDS("*_res.rds")


